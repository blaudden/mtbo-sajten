---
// Persistent announcement banner linking to the WMTBOC26 subsite
---

<div class="w-full bg-primary border-b border-yellow-500/30 text-sm relative overflow-hidden group">
  <!-- Sparkle effect container -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden" aria-hidden="true">
    <div class="absolute top-0 right-10 w-20 h-20 bg-yellow-400/10 rounded-full blur-xl animate-pulse"></div>
    <div
      class="absolute bottom-0 left-20 w-32 h-32 bg-yellow-600/10 rounded-full blur-xl animate-pulse"
      style="animation-delay: 1s;"
    >
    </div>
  </div>

  <div class="max-w-7xl mx-auto px-4 md:px-6 py-2 flex items-center justify-center relative z-10">
    <a
      href="/wmtboc26"
      class="flex items-center gap-3 text-white no-underline group-hover:text-yellow-100 transition-colors"
      aria-label="Open dedicated World Championships 2026 site"
    >
      <span class="sr-only">Click to open the dedicated World Championships 2026 site</span>
      <span class="inline-block">
        <span
          class="bg-gradient-to-r from-yellow-300 via-yellow-500 to-yellow-300 bg-clip-text text-transparent font-bold animate-gradient bg-[length:200%_auto] animate-gold-pulse"
        >
          ✨ Click here to open our dedicated World Championships site ✨
        </span>
        <span class="hidden sm:inline text-white/90 font-medium"> — news, schedules and practical information</span>
      </span>
    </a>
  </div>
  <style>
    @keyframes gradientMove {
      0% {
        background-position: 0% 50%;
      }
      50% {
        background-position: 100% 50%;
      }
      100% {
        background-position: 0% 50%;
      }
    }
    @keyframes goldPulse {
      0%,
      100% {
        text-shadow: 0 0 5px rgba(234, 179, 8, 0.5);
        filter: brightness(1);
      }
      50% {
        text-shadow:
          0 0 15px rgba(234, 179, 8, 0.8),
          0 0 5px rgba(253, 224, 71, 0.8);
        filter: brightness(1.2);
      }
    }
    .animate-gradient {
      animation: gradientMove 3s linear infinite;
    }
    .animate-gold-pulse {
      animation: goldPulse 2s ease-in-out infinite;
    }
  </style>
</div>
