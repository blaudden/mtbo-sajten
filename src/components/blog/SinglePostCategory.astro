---
import { getPermalink } from '~/utils/permalinks';
import { getFormattedDate } from '~/utils/utils';

import { Icon } from 'astro-icon/components';

import { getPrettyCategory } from '~/utils/category';

export interface Props {
  category?: string;
  publishDate: Date;
}

const { category = '', publishDate } = Astro.props;

---


  <Icon name="tabler:clock" class="w-4 h-4 inline-block -mt-0.5 mr-1 dark:text-gray-400" />
  <time datetime={String(publishDate)} class="inline-block">{getFormattedDate(publishDate)}</time>
  {
   category && (
      <>
        {' '}
        ·{' '}
        <a class="capitalize hover:underline inline-block" href={getPermalink(category, 'category')}>
          {getPrettyCategory(category)}
        </a>
      </>
    )
  }
